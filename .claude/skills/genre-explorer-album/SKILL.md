---
name: genre-explorer-album
description: genre-explorer分析で発掘した対極ジャンルの入口アーティストのおすすめアルバムを提示し、ライブラリ保存まで行う。「genre-explorerで上がったアーティストのアルバム教えて」「冒険ジャンルのアーティストのアルバム見たい」「対極ジャンル探索のアルバムをライブラリに保存して」など、genre-explorer推薦結果のアルバム紹介・保存を求められたときに使う。genre-explorerの推薦が会話に既にある場合はその結果を再利用し、ない場合はgenre-explorerスキルを先に実行してからこのスキルを続ける。
---

# genre-explorer-album

genre-explorer で発掘した対極ジャンルの入口アーティストの代表アルバムを紹介し、希望があればライブラリに保存する。

## 前提確認

会話に genre-explorer の推薦結果が既にあれば **Step 2 から始める**。
結果がない場合は genre-explorer スキルを先に実行し、推薦リストを得てから Step 2 へ進む。

---

## Step 1: アーティストリストを確定する

会話中の genre-explorer 推薦からアーティスト名（3〜5人）を抽出する。

---

## Step 2: 各アーティストのアルバムを取得する（並行実行）

各アーティストについて `search_artists` でアーティストIDを取得してから `get_artist_albums` を同時実行する。

```
get_artist_albums(artist_id=<id>, limit=5)
```

> 複数アーティストの取得は必ず並行（同一ターン内で複数ツール呼び出し）で行う。

---

## Step 3: おすすめアルバムを選んで提示する

各アーティストにつき **代表アルバム1〜2枚** を選ぶ。選ぶ基準：
- 対極ジャンルの入門として最も聴きやすいもの
- ユーザーの既存の好みと「意外な接点」が見えるもの

```
## [対極ジャンル名] 冒険アルバム

1. **[アーティスト名]**
   🎵 *[アルバム名]* ([年])
   → [なぜこのアルバムが入口として合うか1文。ロック好きに刺さるポイントがあれば添える]

...（3〜5人分）

---
保存したいアルバムがあれば言ってください。
```

---

## Step 4: ライブラリ保存（希望時のみ）

ユーザーが保存を希望したアルバムを `save_album(album_id=<id>)` でライブラリに追加する。
複数ある場合は並行実行する。

---

## 注意点

- アルバムが見つからない場合は `search_albums` で補完する
- シングルやEPよりフルアルバムを優先する
- 保存の確認は一度だけ。個別確認せずまとめて保存してよい

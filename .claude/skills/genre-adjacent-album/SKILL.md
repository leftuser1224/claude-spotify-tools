---
name: genre-adjacent-album
description: genre-adjacent分析で発掘したギャップジャンルのブリッジアーティストのおすすめアルバムを提示し、ライブラリ保存まで行う。「genre-adjacentで上がったアーティストのアルバム教えて」「ジャンル探索のアーティストのアルバム見たい」「ブリッジアーティストのアルバムをライブラリに保存して」など、genre-adjacent推薦結果のアルバム紹介・保存を求められたときに使う。genre-adjacentの推薦が会話に既にある場合はその結果を再利用し、ない場合はgenre-adjacentスキルを先に実行してからこのスキルを続ける。
---

# genre-adjacent-album

genre-adjacent で発掘したブリッジアーティストの代表アルバムを紹介し、希望があればライブラリに保存する。

## 前提確認

会話に genre-adjacent の推薦結果が既にあれば **Step 2 から始める**。
結果がない場合は genre-adjacent スキルを先に実行し、推薦リストを得てから Step 2 へ進む。

---

## Step 1: アーティストリストを確定する

会話中の genre-adjacent 推薦からアーティスト名（3〜5人）を抽出する。

---

## Step 2: 各アーティストのアルバムを取得する（並行実行）

各アーティストについて `search_artists` でアーティストIDを取得してから `get_artist_albums` を同時実行する。

```
get_artist_albums(artist_id=<id>, limit=5)
```

> 複数アーティストの取得は必ず並行（同一ターン内で複数ツール呼び出し）で行う。

---

## Step 3: おすすめアルバムを選んで提示する

各アーティストにつき **代表アルバム1〜2枚** を選ぶ。選ぶ基準：
- そのアーティストの入門として最適なもの
- リリース年が古すぎず、ジャンルの特徴が凝縮されているもの

```
## [ギャップジャンル名] おすすめアルバム

1. **[アーティスト名]**
   🎵 *[アルバム名]* ([年])
   → [なぜこのアルバムが入口として合うか1文]

...（3〜5人分）

---
保存したいアルバムがあれば言ってください。
```

---

## Step 4: ライブラリ保存（希望時のみ）

ユーザーが保存を希望したアルバムを `save_album(album_id=<id>)` でライブラリに追加する。
複数ある場合は並行実行する。

---

## 注意点

- アルバムが見つからない場合は `search_albums` で補完する
- シングルやEPよりフルアルバムを優先する
- 保存の確認は一度だけ。個別確認せずまとめて保存してよい
